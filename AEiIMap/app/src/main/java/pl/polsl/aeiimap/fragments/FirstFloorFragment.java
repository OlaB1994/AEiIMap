package pl.polsl.aeiimap.fragments;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.MotionEvent;
import android.view.View;
import android.view.ViewGroup;

import butterknife.BindView;
import butterknife.ButterKnife;
import butterknife.OnClick;
import pl.polsl.aeiimap.R;
import pl.polsl.aeiimap.dialogs.InfoDialogFragment;
import pl.polsl.aeiimap.views.ZoomableRelativeLayout;

/**
 * Created by aleksandrabienioszek on 28.02.2017.
 */
public class FirstFloorFragment extends Fragment {

    private final int FIRST_FLOOR = 1;

    @BindView(R.id.fragment_first_floor_main_ll)
    final
    ZoomableRelativeLayout zoomableRelativeLayout;

    @OnClick(R.id.fragment_first_floor_101)
    public void on101() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 101).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_102)
    public void on102() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 102).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_103)
    public void on103() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 103).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_104)
    public void on104() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 104).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_105)
    public void on105() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 105).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_106)
    public void on106() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 106).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_107)
    public void on107() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 107).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_108)
    public void on108() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 108).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_109)
    public void on109() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 109).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_110_111)
    public void on110() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 110).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }


    @OnClick(R.id.fragment_first_floor_112)
    public void on112() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 112).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_113)
    public void on113() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 113).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_114)
    public void on114() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 114).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_115)
    public void on115() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 115).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_116a)
    public void on116a() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 116).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_116b)
    public void on116b() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 116).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_117)
    public void on117() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 117).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_118)
    public void on118() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 118).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_119a)
    public void on119a() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 119).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_119b)
    public void on119b() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 119).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_120)
    public void on120() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 120).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_121)
    public void on121() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 121).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_122)
    public void on122() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 122).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_123)
    public void on123() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 123).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_124)
    public void on124() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 124).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_125)
    public void on125() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 125).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_126)
    public void on126() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 126).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_127)
    public void on127() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 127).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_128)
    public void on128() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 128).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_129)
    public void on129() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 129).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_130)
    public void on130() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 130).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_131)
    public void on131() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 131).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_131a)
    public void on132() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 132).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_bufet)
    public void onBufet() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 1006).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_aula_a)
    public void onAulaA() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 1001).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_aula_b)
    public void onAulaB() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 1002).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_aula_c)
    public void onAulaC() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 1003).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_work_wc)
    public void onWorkerWC() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 1007).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_first_floor_132)
    public void on132b() {
        InfoDialogFragment.newInstance(getContext(), FIRST_FLOOR, 1008).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }


    @Nullable
    @Override
    public View onCreateView(LayoutInflater inflater,
                             @Nullable ViewGroup container,
                             @Nullable Bundle savedInstanceState) {
        View view = inflater.inflate(R.layout.fragment_first_floor, container, false);
        ButterKnife.bind(this, view);
        zoomableRelativeLayout.setOnTouchListener(new View.OnTouchListener() {
            @Override
            public boolean onTouch(View v, MotionEvent event) {
                zoomableRelativeLayout.init(getContext());
                return false;
            }
        });
        return view;
    }
}
