package pl.polsl.aeiimap.fragments;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import butterknife.ButterKnife;
import butterknife.OnClick;
import pl.polsl.aeiimap.R;
import pl.polsl.aeiimap.dialogs.InfoDialogFragment;

/**
 * Created by aleksandrabienioszek on 28.02.2017.
 */
public class SecondFloorFragment extends Fragment {

    private final int SECOND_FLOOR = 2;

    @OnClick(R.id.fragment_second_floor_201)
    public void on201() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 201).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_203)
    public void on203() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 203).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_204)
    public void on204() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 204).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_205)
    public void on205() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 205).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_206)
    public void on206() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 206).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_207)
    public void on207() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 207).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_208)
    public void on208() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 208).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_209)
    public void on209() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 209).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_210)
    public void on210() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 210).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_211)
    public void on211() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 211).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_211b)
    public void on211b() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 211).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_212)
    public void on212() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, "212a").show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_212b)
    public void on212b() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, "212a").show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_213)
    public void on213() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 213).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_214)
    public void on214() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 214).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_215)
    public void on215() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 215).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_215a)
    public void on215a() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 215).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_216)
    public void on216() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 216).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_217)
    public void on217() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 217).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_218)
    public void on218() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 218).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_219)
    public void on219() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 219).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_220)
    public void on220() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 220).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_221)
    public void on221() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 221).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_237)
    public void on237() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 237).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_238)
    public void on238() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 238).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_239)
    public void on239() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 239).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_240)
    public void on240() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 240).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_241)
    public void on241() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 241).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());
    }

    @OnClick(R.id.fragment_second_floor_aula_f)
    public void onAulaF() {
        InfoDialogFragment.newInstance(getContext(), SECOND_FLOOR, 222).show(getActivity().getFragmentManager(),
                InfoDialogFragment.class.getName());

    }

    @Nullable
    @Override
    public View onCreateView(LayoutInflater inflater,
                             @Nullable ViewGroup container,
                             @Nullable Bundle savedInstanceState) {
        View view = inflater.inflate(R.layout.fragment_second_floor, container, false);
        ButterKnife.bind(this, view);
        return view;
    }
}
